# Software Train simulator
Want to test the API & user interface but don't want to use the train hardware? This app emulates the train firmware and serves the same pages.

## Install
`npm install`

## Run
`node index.js`